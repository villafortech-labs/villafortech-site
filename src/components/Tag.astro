---
interface Props {
  size?: 'sm' | 'md';
}

const { size = 'md' } = Astro.props;
---

<span
  class:list={[
    'inline-block bg-gray-100 text-gray-700 rounded-full font-medium',
    size === 'sm' ? 'px-2 py-0.5 text-xs' : 'px-3 py-1 text-sm',
  ]}
>
  <slot />
</span>
